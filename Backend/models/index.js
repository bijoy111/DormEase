// add models
auth_model = require('./auth.model');
dashboard_model = require('./dashboard.model');
complaint_model = require('./complaint.model');

module.exports = {
    // add models
    auth_model,
    dashboard_model,
    complaint_model
}